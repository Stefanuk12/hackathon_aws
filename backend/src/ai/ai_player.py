"""STRETCH: the AI draws its own entry with Nova Canvas and is judged blind.

Check Nova Canvas is available in the stack's region before building this.
TODO person 4: bedrock.invoke_model(IMAGE_MODEL_ID, TEXT_IMAGE task) -> JPEG -> S3,
then submit it as player "ai".
"""
